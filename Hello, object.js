// jshint esversion: 6
// jshint asi: true
const user = {} // creates an empty object user
user.name = 'John' // adds the property name with the value John
user.surname = 'Smith' // adds the property surname with the value Smith
user.name = 'Pete' // changes the value of the name to Pete
delete user.name // removes the property name from the object
